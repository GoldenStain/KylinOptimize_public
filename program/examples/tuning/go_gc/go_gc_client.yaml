project: "go_gc"
engine : "bayes"
iterations : 50
random_starts : 20

benchmark : "sh /home/wsw/桌面/KylinDBOptimize/program/examples/tuning/go_gc/go_gc.sh 3"
evaluations :
  -
    name: "ns/op"
    info:
        get: "echo '$out' | awk '{print $3}' | awk '{if($1!~/s$/)print}' | awk '{sum+=$1}END{print sum/3}'"
        type: "negative"
        weight: 100
        threshold: 100
  -
    name: "B/op"
    info:
        get: "echo '$out' | awk '{print $6}' | awk '{if($1!~/B$/)print}' | awk '{sum+=$1}END{print sum/3}'"
        type: "negative"
        weight: 50
        threshold: 100
  -
    name: "allocs/op"
    info:
        get: "echo '$out' | awk '{print $9}' | awk '{if($1!~/allocs$/)print}' | awk '{sum+=$1}END{print sum/3}'"
        type: "negative"
        weight: 50
        threshold: 100